# The frontend talks to the backend through Vite's dev proxy (/api -> :5000),
# so no API URL is needed in development.
#
# Runtime keys (Google Maps, Paystack public key) are fetched from the backend
# at /api/config, so they do not need to be duplicated here.
